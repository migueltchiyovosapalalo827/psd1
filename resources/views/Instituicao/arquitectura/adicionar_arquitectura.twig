{% extends "@include/main_painel.twig" %}

    {% block conteudo %}

        <div class="card">
            <div class="card-body">
                <h2 class="mt-2">
                    Adicionar Arquitectura
                </h2>
                <hr>
        <form class="w-100" id="needs-validation" action="salvar_arquitectura"  method="POST">
        {{ csrf_field() }}
                 	<div class="form-group col-md-8">
                        <input type="hidden" value="{{ usuario.id }}" name="id_instituicao">

                            <div class="form-group col-md-5">
                            <label for="rsNome">Arquitectura </label>
                            <input type="text" class="form-control"    value="" name="arquitectura">
                            <!--<input type="hidden" class="form-control" value="{{ historial.id }}" name="id" id="rsNome">!-->
                        </div>
                        </div>
                 	 <div class="d-flex align-items-end justify-content-end">
                        <button type="submit" class="btn btn-success" value="Salvar">Salvar</button>
                    </div>
        </form>
        </div>
    </div>


    {% endblock %}
